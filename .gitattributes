# Normalize line endings across platforms.
* text=auto eol=lf

# Windows scripts must keep CRLF.
*.bat text eol=crlf

# The Gradle wrapper script must stay LF or it won't run on Unix/macOS.
gradlew text eol=lf

# Binary assets — never touch line endings or try to diff.
*.png binary
*.jar binary
